syntax = "proto3";

message DeployTx {
    // Code plus input arguments to constructor
    bytes input = 1;
}

message SendTx {
    // Contract address
    bytes address = 1;
    // Function name plus input arguments
    bytes input = 2;
}
